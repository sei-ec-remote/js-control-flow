let x = 56 // Assign a random number variable here
let y = 25 // Assign another random number variable here

if(x > y) {
    console.log(`The larger number of ${x} and ${y} is ${x}`)
} else if(y > x) {
    console.log(`The larger number of ${x} and ${y} is ${y}`)
}