let age = 15
if (age <= 3) {
    console.log(`dang, yer a lil infant at ${age}`)
} else if (age <=6) {
    console.log(`ye sure have grown! ${age}? yer a toddler now!`)
} else if (age <= 9) {
    console.log(`yep. sure are in that annoying range... ${age} means yer a child`)
} else if (age <= 12) {
    console.log(`yes'm, sure are lookin' inta those bad years at ${age}. yer a preteen now`)
} else if (age <= 18) {
    console.log(`${age}? a teen? Hooooooo-wee! hope ya like bein' 'spected ta act like an adult while we all treat ya like a child!`)
} else if (age <= 20) {
    console.log(`welcome to bin' a young adult! If ya thing ${age} is bad, wait till ya gotta pay taxes!`)
}
