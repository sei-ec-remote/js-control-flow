let thing = "crocodile"
let count = 0

if (count > 0) {
    if (count > 1) {
        console.log(`There are ${count} ${thing}s here right now.`)
    } else {
        console.log(`There is ${count} ${thing} here right now.`)
    }
} else {
    console.log(`Not sure what you mean, I don't see any ${thing}s around here.`)
}