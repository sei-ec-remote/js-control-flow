// Declare initial variables
let thing = 'cat';
let count = 0;

// Determine if the 'thing' should be pluralized then print out pluralized version
if(count === 1){
    console.log(count + ' ' + thing)
} else {
    console.log(count + ' ' + thing + 's')
}
