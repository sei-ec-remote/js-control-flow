// Defining thing and count variables
let thing = 'cat';
let count = 5;

// Printing pluralized form of thing variable based on count variable
console.log(count === 1 ? `${count} ${thing}` : `${count} ${thing}s`);