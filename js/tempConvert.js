//Write a program that converts a
// temperature from Fahrenheit to another temperature unit indicated by a variable.

let tempF= 10
let temp = 'c' 

if ( temp ==='c') {
    console.log("The Temperature is" + (Math.round((tempF - 32) * 5/9)) + " Degrees Celcius")
}
