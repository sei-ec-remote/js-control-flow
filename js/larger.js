//add numbers to compare
let uno = 32
let dos = 15

//compare the numbers providing the larger number.
if (uno > dos) {
    console.log("The larger number of " + uno + " and " + dos + " is " + uno);
} else if (uno < dos) {
    console.log("The larger number of " + uno + " and " + dos + " is " + dos);
}else {
    console.log(" The numbers are equal")
}