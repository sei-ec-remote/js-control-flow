//create two variables with number values
let num1 = 8
let num2 = 3
//create conditions for first number being larger than the second and
// the second number being larger than the first
if (num1 > num2) {
    console.log(`The larger number of ${num1} and ${num2} is ${num1}`)
}
else if (num1 < num2) {
    console.log(`The larger number of ${num1} and ${num2} is ${num2}`)
}
//finish condition for equality
else  {
    console.log(`${num1} and ${num2} are equal`)
}