let varA = 100
let varB = 100

if (varA === varB) {
  console.log(`${varA} and ${varB} are equal`)
} else if (varA < varB) {
  console.log(`${varB} is larger than ${varA}`)
} else {
  console.log(`${varA} is larger than ${varB}`)
}